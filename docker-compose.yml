version: "3.5"
   
services:

  nginx:
      build: 
        context: ./nginx
      container_name: nginx
      links:
        - uwsgi  
      volumes_from:
        - uwsgi
      ports:
        - "8000:8000"
                

  uwsgi:
      build: 
        context: ./university-checker
      container_name: uwsgi
      volumes:
        - ./nginx:/opt/conf
        - ./university-checker:/opt/app
      expose:
        - "8001"
        
